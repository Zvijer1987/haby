# Haby runtime example
# Copy values into your own compose file or environment management flow.

NODE_ENV=production
PORT=3000

# Cookie security:
# false = allow HTTP (local testing via IP:port)
# true  = require HTTPS (recommended behind reverse proxy)
COOKIE_SECURE=true
